'use strict';

const assert = require('assert');
// const oniyiHttpClient = require('../lib');

describe('oniyi-http-client', () => {
  it('should have unit test!', () => {
    assert(false, 'we expected this package author to add actual unit tests.');
  });
});
